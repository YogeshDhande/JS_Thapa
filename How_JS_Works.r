1.Our JS code
2.parsing --
        Lexical parser --- JS code into Tokens(e.x-Variables, operator...)
        Syntax Parser --- check for syntax & create AST(Abstract Syntax Tress)(Parse Tree)
3.compilation 
        JIT(Just In Compile) --- JS code into machine code/byte code
4.Execution Phase
        call Stack & heap memory
        Hosting & Scope Chain are created here.
5.Final Output